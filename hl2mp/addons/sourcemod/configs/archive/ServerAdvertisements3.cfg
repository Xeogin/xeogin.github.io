"ServerAdvertisements3"
{
  "Settings"
  {
    "ServerName" "[{green}Xeogin's Compendium{default}]" // Server tag
    "ServerType" "default" // Only if MySQL is 1 -> Simply divide messages to groups (e.g. 3 servers uses ServerType surf and 2 servers uses jailbreak)
    "Time" "30" // Time in seconds
    "Languages"  "EN" // List of languages / Keep the main language first (If player connect from Country you did not translated. He will get messages in language which is the first in list).
    "Default language" "ingame" // Value = "geoip" or "ingame"
    "Log expired messages" "0" // 1 = Enabled -> Messages are logged in Sourcemod's error log
  }
  "Welcome Message"
  {
    "Enabled" "1" // 1 = Enabled, 0 = Disabled
    "Type" "T" // Type of message - Only T and C types are supported right now
    "en" "[{green}Console{default}] Welcome to the server {PLAYERNAME}" \n 
    "Delay" "4.0" // Time in seconds after is client connected to server to show message
    "flags" "" // Client with this flag will get welcome message - Keep empty for all
  }
  "Messages"
  {
    "1"
    {
      "maps" "all" //- Default: all
      "en""{ghostwhite}To change your character model, type '{green}!models{ghostwhite}' in the chat"
      "type" "T"
      "tag" "" // Message without tag
      "ignore_maps" "de_inferno;ar_"
    }
    "2"
    {
      "maps" "all" //- Default: all
      "en""{ghostwhite}To start a vote to change the map, type '{green}rtv{ghostwhite}' in the chat"
      "type" "T"
      "tag" "" // Message without tag
      "ignore_maps" "de_inferno;ar_"
    }
    "3"
    {
      "maps" "all" //- Default: all
      "en""{ghostwhite}To just watch for a while, type '{green}spectate{ghostwhite}' in the chat"
      "type" "T"
      "tag" "" // Message without tag
      "ignore_maps" "de_inferno;ar_"
    }
  }
}